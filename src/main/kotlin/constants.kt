class Constants{
    companion object {
        val operators= arrayOf("Token(token=DOT_SAFE)", "Token(token=ELVIS)", "Token(token=RANGE)")
        val scopingFunction= arrayOf("Name(name=run)", "Name(name=let)", "Name(name=also)", "Name(name=apply)", "Name(name=with)")
    }
}